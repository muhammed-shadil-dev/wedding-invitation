# Optional. Forward every RSVP to an endpoint you control — a Google Apps
# Script web app, a Zapier catch hook, a Slack incoming webhook, or your own
# API. Without it, responses are still written to data/rsvp.jsonl.
# RSVP_WEBHOOK_URL=https://script.google.com/macros/s/.../exec
